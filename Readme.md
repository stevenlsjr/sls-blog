# Yet another attempt at a nodejs blog platform



to run:

```bash
cp packages/strapi-server/.env.example packages/strapi-server/.env
# modify env file as needed
yarn install
yarn workspace @sls-blog/strapi-server develop
```